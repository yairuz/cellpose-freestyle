version: '3.8'

services:
  cellpose-freestyle:
    build:
      context: ..
      dockerfile: app/Dockerfile
    container_name: cellpose-freestyle
    ports:
      - "5000:5000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      # Mount for development (optional - comment out for production)
      - ./backend.py:/app/backend.py
      - ./static:/app/static
      # Persistent storage for uploads
      - ./uploads:/app/uploads
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Optional: Add nginx reverse proxy for production
  # nginx:
  #   image: nginx:alpine
  #   container_name: cellpose-nginx
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #   depends_on:
  #     - cellpose-freestyle
  #   restart: unless-stopped
